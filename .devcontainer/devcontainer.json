{
  "name": "webapp fullstack devcontainer",
  "image": "mcr.microsoft.com/devcontainers/node:20",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/python:1": {}
  },
  "forwardPorts": [5173, 8000],
  "postCreateCommand": "cd webapp-ui && npm install && npm run build && cd ../webapp-server && pip install --upgrade pip && pip install -r requirements.txt",
  "postStartCommand": "(cd webapp-ui && nohup npm run preview -- --host 0.0.0.0 &) && (cd webapp-server && nohup python app.py &)",
  "workspaceFolder": "/workspaces/webapp"
}
